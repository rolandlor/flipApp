import { createAction } from '@ngrx/store';

export const flip = createAction('[Card Component] Flip');
export const unflip = createAction('[Card Component] Unflip');
export const reset = createAction('[Card Component] Reset');
